{
  "name": "runtime",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "node --loader=ts-node/esm build.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.23.1"
  },
  "dependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}