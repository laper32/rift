// Workspace-level dependencies
// These are available to all workspace members via `source: "workspace"`

addDependency({ name: "github.com/gin-gonic/gin", version: "1.9.1" });
addDependency({ name: "github.com/stretchr/testify", version: "1.8.4" });
